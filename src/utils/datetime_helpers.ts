import { FormatDateOptions } from "react-intl";
export const DATE_WITH_TIME_FORMAT = {
  day: "numeric",
  month: "short",
  year: "numeric",
  hour: "numeric",
  minute: "numeric",
} as FormatDateOptions;

export const DATE_FORMAT = {
  day: "numeric",
  month: "short",
  year: "numeric",
};

export const TIME_FORMAT = {
  hour: "numeric",
  minute: "numeric",
};
